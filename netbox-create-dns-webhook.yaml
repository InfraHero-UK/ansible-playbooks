---
- name: Test Webhook from Netbox
  hosts: localhost
  connection: local
  gather_facts: false
  become: false

  tasks:
    - name: Debug DNS Record
      debug:
        msg: "Zone: {{ netbox_data.zone.name }}"
