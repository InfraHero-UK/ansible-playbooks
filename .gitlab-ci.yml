stages:
  - update
update:
  stage: update
  script:
    - apk add --no-cache curl
    - curl -X POST --user "gitlab:${GITLAB_TOKEN}" https://awx.infrahero.co.uk/api/v2/projects/18/update/ -k -L
  only:
    - main
